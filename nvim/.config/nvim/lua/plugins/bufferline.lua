require('bufferline').setup {
  options = {
    diagnostics = "nvim_lsp"
    --separator_style = 'slant',
    -- hover = {
    --   enabled = true,
    --   delay = 200,
    --   reveal = {'close'}
    -- },
  }
}
